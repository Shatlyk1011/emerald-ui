emerald-ui:

- discord components

Global:

- add what's new section in main layout

in entire layout, i want add some info button in the right side of layout so when user clicks it, it can show whats new were added to the side. the news section will contain gsap or motion components with links. the data will be static - updatable from code side.


docker run -it --rm --name n8n -p 5678:5678 -v n8n_data:/home/node/.n8n docker.n8n.io/n8nio/n8n
